[package]
name = "smartme-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false
description = "The smartme_mqtt bridge application: canonical Measurement, state machine, adapters, web UI, wiring."

[features]
# The panicking route Story 6.1 AC5 needs to assert its second half ([#51]).
# Off by default and never enabled by `docker-publish.yml`, so no released image
# carries it. `ci-local.sh` and `ci.yml` run the one test that turns it on.
panic-probe = []

[dependencies]
sparkplug-b = { path = "../sparkplug-b" }
smart-me-client = { path = "../smart-me-client" }
serde = { workspace = true }
toml = { workspace = true }
tokio = { workspace = true }
rumqttc = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tracing-appender = { workspace = true }
thiserror = { workspace = true }
arc-swap = { workspace = true }
axum = { workspace = true }
# For `--healthcheck` alone ([ADR 0041], story 7.1): one GET against this bridge's
# own `/healthz`. **This adds nothing to the image** — `smart-me-client` already
# links it, so the crate is compiled into this binary either way; what changes is
# that the dependency is now declared where it is used rather than borrowed
# through another crate's public surface.
reqwest = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
tokio = { workspace = true, features = ["test-util"] }
testcontainers = { workspace = true }
